// Imprimir os 10 primeiros números ímpares;

/* 
let nImpares = 1;

while (nImpares <= 20) {
    if (nImpares % 2 == 1) {
        console.log(nImpares);
    }
    nImpares += 1;
} */

let nImpares = 1;

while (nImpares <= 20) {
    console.log(nImpares);
    nImpares = nImpares + 2;
}